<%= form_for(@job) do |f| %>
  <div class="field">
    <%= render 'shared/error_messages', object: f.object %>
    <%= f.label 'Job Details' %>
    <%= f.text_area :content, placeholder: "Fill in job details..." %>
  </div>
  <%= f.submit "Post" %>
<% end %>
