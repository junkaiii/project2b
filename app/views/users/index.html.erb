<p id="notice"><%= notice %></p>

<h1>Users</h1>
<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Password</th>
      <th colspan="3"></th>
    </tr>
  </thead>
    <%= render @users %>
  <tbody>
    <% @users.each do |user| %>

    <% end %>
  </tbody>
</table>
<%= will_paginate %>

<br>

<%= link_to 'New User', new_user_path %>
